@model List<ToyPlanet.Core.CartItem>
@{
    ViewData["Title"] = "Кошик";
    var total = Model.Sum(item => item.Toy != null ? item.Toy.Price * item.Quantity : 0);
}

<div class="container mt-4">
    <h2 style="color:#e75480;">🛒 Кошик</h2>
    
    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">@TempData["Success"]</div>
    }
    
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-warning">@TempData["Error"]</div>
    }
    
    @if (!Model.Any())
    {
        <div class="alert alert-info mt-4">Ваш кошик порожній.</div>
    }
    else
    {
        <table class="table table-bordered bg-white mt-4">
            <thead class="table-light">
                <tr>
                    <th>Товар</th>
                    <th>Ціна</th>
                    <th>Кількість</th>
                    <th>Сума</th>
                    <th>Дії</th>
                </tr>
            </thead>
            <tbody>
            @foreach (var item in Model)
            {
                if (item.Toy != null)
                {
                    <tr>
                        <td>
                            <div class="d-flex align-items-center">
                                <img src="/images/pony@(item.ToyId).png" alt="@item.Toy.Name" style="width:60px; height:60px; object-fit:cover; margin-right:12px; border-radius:8px;">
                                <strong>@item.Toy.Name</strong>
                            </div>
                        </td>
                        <td>@item.Toy.Price ₴</td>
                        <td>
                            <div class="d-flex align-items-center">
                                <form method="post" asp-action="Remove" style="display:inline">
                                    <input type="hidden" name="id" value="@item.ToyId" />
                                    <button class="btn btn-sm btn-outline-secondary" title="Зменшити кількість">−</button>
                                </form>
                                <span class="mx-2">@item.Quantity</span>
                                <form method="post" asp-action="Add" style="display:inline">
                                    <input type="hidden" name="id" value="@item.ToyId" />
                                    <button class="btn btn-sm btn-outline-secondary" title="Збільшити кількість">+</button>
                                </form>
                            </div>
                        </td>
                        <td><strong>@(item.Toy.Price * item.Quantity) ₴</strong></td>
                        <td>
                            <form method="post" asp-action="RemoveAll" style="display:inline">
                                <input type="hidden" name="id" value="@item.ToyId" />
                                <button class="btn btn-sm btn-danger" title="Видалити з кошика">
                                    <i class="bi bi-trash"></i> Видалити
                                </button>
                            </form>
                        </td>
                    </tr>
                }
            }
            </tbody>
            <tfoot class="table-light">
                <tr>
                    <th colspan="3" class="text-end">Загальна сума:</th>
                    <th><span style="color:#e75480; font-size:1.2rem;">@total ₴</span></th>
                    <th></th>
                </tr>
            </tfoot>
        </table>
        
        <div class="mt-4 mb-4">
            <a href="/Cart/Checkout" class="btn btn-success btn-lg" style="background:#4caf50; border:none;">
                📦 Оформити замовлення
            </a>
        </div>
    }
    
    <a href="/Catalog" class="btn btn-link mt-3">← Продовжити покупки</a>
</div>
